# Optional VPN override — route ONLY qBittorrent traffic through WireGuard.
# Usage: docker compose -f docker-compose.yml -f docker-compose.vpn.yml up -d
#
# Place your WireGuard client config at ./data/wireguard/wg0.conf
# The VPN server should port-forward the torrent listen port (default 6891).
#
# Architecture:
#   wireguard ←──network_mode──→ qbittorrent  (VPN tunnel)
#       │
#       └── internal network ──→ bot, telegram-bot-api, qb-proxy (normal internet)
#
# Bot connects to qB via "wireguard:8080" and telegram-bot-api via "telegram-bot-api:8081"
# External access to qB WebUI via qb-proxy on port 8080

services:
  wireguard:
    image: linuxserver/wireguard:latest
    restart: unless-stopped
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    sysctls:
      - net.ipv4.conf.all.src_valid_mark=1
      - net.ipv6.conf.all.disable_ipv6=1
    dns:
      - 1.1.1.1
      - 8.8.8.8
    environment:
      - PUID=0
      - PGID=0
      - TZ=Asia/Taipei
    volumes:
      - ./data/wireguard:/config
    ports:
      - "6891:6891"
      - "6891:6891/udp"
    networks:
      - internal
    healthcheck:
      test: ["CMD", "wg", "show", "wg0"]
      interval: 10s
      timeout: 5s
      retries: 3

  qbittorrent:
    network_mode: "service:wireguard"
    depends_on:
      wireguard:
        condition: service_healthy
    ports: !reset []

  # Reverse proxy for qB WebUI (because wireguard container can't route responses to external clients)
  qb-proxy:
    image: alpine/socat:latest
    restart: unless-stopped
    command: TCP-LISTEN:8080,fork,reuseaddr TCP:wireguard:8080
    ports:
      - "8080:8080"
    networks:
      - internal
    depends_on:
      wireguard:
        condition: service_healthy

  # Reverse proxy for Stream Server (bot container)
  stream-proxy:
    image: alpine/socat:latest
    restart: unless-stopped
    command: TCP-LISTEN:8082,fork,reuseaddr TCP:bot:8082
    ports:
      - "8082:8082"
    networks:
      - internal
    depends_on:
      - bot

  telegram-bot-api:
    network_mode: !reset ""
    depends_on:
      - qbittorrent
    networks:
      - internal
    ports:
      - "8081:8081"

  bot:
    network_mode: !reset ""
    depends_on:
      - qbittorrent
      - telegram-bot-api
    networks:
      - internal
    environment:
      - QB_HOST=wireguard
      - TELEGRAM_API_ROOT=http://telegram-bot-api:8081

networks:
  internal:
    driver: bridge
